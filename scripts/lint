#!/bin/sh -e

# Apply every auto-fix the CI checks for: format and lint Python, format Zig.

export SOURCE_FILES="zttp tests bench.py"

set -x

uv run --no-sync ruff format $SOURCE_FILES
uv run --no-sync ruff check --fix $SOURCE_FILES
zig fmt src build.zig
